{
  "name": "wikipedia-quick-search",
  "version": "1.0.0",
  "description": "Sichere Wikipedia-Browsererweiterung: markierten Text per Alt+W/Option+W im Side Panel nachschlagen. Ohne LLM, ohne Tracking, ohne eigenen Server.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "eslint src tests",
    "typecheck": "tsc --noEmit",
    "test": "vitest run tests/unit tests/integration",
    "test:security": "vitest run tests/security",
    "test:all": "vitest run",
    "test:e2e": "playwright test",
    "lint:webext": "npx --yes web-ext@8 lint --source-dir dist/firefox --warnings-as-errors=false",
    "build:chromium": "node scripts/build.mjs chromium",
    "build:firefox": "node scripts/build.mjs firefox",
    "build:safari": "node scripts/build.mjs safari",
    "build:all": "node scripts/build.mjs chromium && node scripts/build.mjs firefox && node scripts/build.mjs safari",
    "package:chromium": "node scripts/build.mjs chromium && node scripts/package.mjs chromium",
    "package:firefox": "node scripts/build.mjs firefox && node scripts/package.mjs firefox",
    "package:safari": "node scripts/build.mjs safari && node scripts/package.mjs safari",
    "audit:security": "npm audit --omit=dev"
  },
  "dependencies": {
    "dompurify": "^3.4.14",
    "pdfjs-dist": "^4.10.38"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.63.0",
    "@types/chrome": "^0.3.0",
    "@types/node": "^26.6.1",
    "adm-zip": "^0.6.1",
    "esbuild": "^0.28.2",
    "eslint": "^10.10.0",
    "jsdom": "^30.1.0",
    "typescript": "^7.0.2",
    "typescript-eslint": "^8.70.0",
    "vitest": "^5.0.1"
  },
  "overrides": {
    "postcss": "^8.5.26",
    "js-yaml": "^5.4.1"
  },
  "allowScripts": {
    "esbuild@0.25.12": true,
    "esbuild@0.28.1": true
  }
}
